<h1><%=t'New saving'%></h1>

<%= form_for(@saving) do |f| %>
  <%= render "shared/errors", object: f.object %>

  <p>
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </p>
  <p>
    <%= f.submit t'Create' %>
  </p>
<% end %>

<%= link_to t('Back'), savings_path %>