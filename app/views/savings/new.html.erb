<h1><%=t'New saving'%></h1>

<% form_for(@saving) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </p>
  <p>
    <%= f.submit t'Create' %>
  </p>
<% end %>

<%= link_to t('Back'), savings_path %>