<h1><%= t 'Current month' %></h1>
<table class="overview">
  <tr>
    <th><%= t 'Fixed incomes' %></th>
    <th><%= t 'Fixed expenses' %></th>
    <th><%= t 'Expenses' %></th>
    <th><%= t 'Incomes' %></th>
    <th><%= t 'Savings' %></th>
    <th><%= t 'Balance' %></th>
  </tr>
  <tr>
    <td><%=h price_formatted @fixed_incomes_amount%></td>
    <td><%=h price_formatted @current_month_fixed_expenses_amount%></td>
    <td><%=h price_formatted @current_month_expenses_amount%></td>
    <td><%=h price_formatted @current_month_incomes_amount%></td>
    <td><%=h price_formatted @savings%></td>
    <td><%=h price_formatted @balance%></td>
  </tr>
</table>
<br />
<table class="overview">
  <tr>
    <% i = 1
    @weekly_balance.each do |wb| %>
      <td>Týden <%= i %>:&nbsp;&nbsp;&nbsp;<%=h price_formatted wb %></td>
    <% 
    i +=1 
    end %>
  </tr>
</table>
<%= link_to t('New expense'), new_expense_path %>
<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Date</th>
  </tr>

<% @current_month_expenses.each do |expense| %>
  <tr>
    <td><%=h expense.name %></td>
    <td class="right"><%=h price_formatted expense.price %></td>
    <td><%=h expense.date %></td>
    <td><%= link_to t('Show'), expense %></td>
    <td><%= link_to t('Edit'), edit_expense_path(expense) %></td>
    <td><%= link_to t('Destroy'), expense, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
<tr>
  <th>Total: </th>
  <th class="right"><%= price_formatted @current_month_expenses_amount %></th>
  <th></th>
</tr>
</table>

<br />

<%= link_to t('New expense'), new_expense_path %>